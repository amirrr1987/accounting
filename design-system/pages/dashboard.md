# Dashboard overrides

- First viewport: brand + one status line + balance equation signal
- Cards are navigation shortcuts (not decorative stats only)
- Empty recent lists must include outcome-focused CTA copy from `ux-copy.ts`
- Prefer `PageHeader` + `EmptyState` + `hy-surface` tokens
